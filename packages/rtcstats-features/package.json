{
  "name": "@rtcstats/rtcstats-features",
  "private": true,
  "version": "1.0.0",
  "author": "rtcstats.com",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "eslint *.js",
    "coverage": "c8 --reporter html --reporter lcov mocha test",
    "test": "npm run lint && npm run coverage",
    "start": "npm i && NODE_CONFIG_DIR=../rtcstats-server/config/ node app.js"
  },
  "dependencies": {
    "config": "^4.1.1"
  }
}
